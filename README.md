# leonardo-deploy

This repo is a service deploy repo for the <service-name> service.
Use `git clone --recurse-submodules` to clone it so that you get
the submodules as well.
For details on how to create infrastructure based on these profiles,
see  the [docs](https://github.com/broadinstitute/dsp-k8s-deploy)
